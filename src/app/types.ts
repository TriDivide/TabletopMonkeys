interface IUser {
    name: String,
    emailAddress: String,
    userId: String,
}

interface ICampaign {
    name: String,
    campaignId: String,
    members: String,
    dungeonMasterId: String,
}